# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
aiofiles==24.1.0
websockets==14.1

# OCR
paddleocr==2.9.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# HuggingFace stack
# transformers latest (4.49+) required by OmniVoice for HiggsAudioV2TokenizerModel
# Python 3.12 (Ubuntu 24.04) has sys.get_int_max_str_digits so torch._dynamo works fine
transformers>=4.49.0
accelerate>=0.30.0
huggingface_hub>=0.22.0

# TTS
omnivoice

# Audio processing
pydub==0.25.1
soundfile==0.12.1

# Text diff
python-Levenshtein==0.26.1

# NOTE: torch, torchaudio installed in Dockerfile from PyTorch cu130 wheel index
